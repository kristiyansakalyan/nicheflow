datamodule:
  _target_: nicheflow.datamodules.microenv_datamodule.MicroEnvDataModule
  seed: ${seed}
  k_regions: 64
  n_microenvs_per_slice: 256
  resample_n_microenvs: 64
  ot_lambda: 0.1
  val_upsample_factor: 4
  train_batch_size: 2
  num_workers: 2

coord_dim: 2
